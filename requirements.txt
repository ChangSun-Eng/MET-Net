# MET-Net reproducibility environment (Python 3.10).
# Pinned versions used to produce the published results.
# CPU is sufficient for all reproduction commands; for GPU training install a matching
# CUDA wheel, e.g. pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu126
torch==2.6.0
numpy==1.26.4
pandas==2.3.2
scikit-learn==1.7.2
scipy==1.15.3
captum==0.8.0
shap==0.49.1
matplotlib==3.10.6
seaborn==0.13.2
openpyxl==3.1.5
joblib==1.5.2
tqdm==4.67.1
# Optional (Stage-1 SLSP-Net physics/tree-guided pretraining only):
xgboost
lightgbm
